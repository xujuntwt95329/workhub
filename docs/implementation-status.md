# 首版实现状态

原始产品/系统设计继续作为方向；2026-09-19 的任务归属与追溯更新以 [task-traceability.md](task-traceability.md) 为准。本文件区分已运行的能力与后续扩展。

Claude 插件、设置页下载与安装步骤已实现，含 7 个 Skills 和 MCP 连接配置，详细能力及客户端验证边界见 [claude-plugin.md](claude-plugin.md)。

| 范围     | 当前实现                                                                              | 后续扩展                                          |
| -------- | ------------------------------------------------------------------------------------- | ------------------------------------------------- |
| 部署     | 单工作空间、单 Owner；开发 PGlite，生产 PostgreSQL；HTTPS Compose                     | 多用户、多租户、正式升级迁移管理                  |
| 工程内容 | 任务、分组/折叠需求、拒绝/恢复/回收站、验收标准、多设计、实施、问题、疑问、版本与评审 | 需求→设计→代码细粒度映射、批量评审、评论讨论      |
| 想法     | 全局/任务内快速录入、归属任务、编辑、状态、转任务和源记录保留                         | 合并重复、跨项目迁移                              |
| 原则     | 任务作用域、旧作用域无损迁移、版本基线、采用更新、按代码版本核对必需原则              | 例外审批、复杂原则冲突分析                        |
| 质量     | 结构化用例、双向追溯、关联缺陷、追加结果、需求版本失效、门槛、豁免 API、不适用 UI     | 附件上传、CI 签名核验、覆盖率文件直接导入         |
| Agent    | REST、JSON Schema、MCP；Bearer/OAuth、作用域、幂等、乐观锁                            | Agent 自动启动和编排、Webhook、Git 托管集成       |
| 助手     | 自配模型、只读问答/摘要、引用、队列、预算与恢复                                       | 自主写入的审批执行、多轮记忆、RAG、token 成本统计 |
| 报告     | 实时矩阵、CSV/Markdown/JSON、手动快照、验收自动快照                                   | 周报定时投递、定制仪表盘、长期趋势                |
| 备份     | JSON 业务导出、pg_dump/pg_restore 脚本                                                | UI 导入、对象存储、自动备份计划                   |

测试包含纯业务规则、真实嵌入式 PostgreSQL 服务集成、API/MCP/OAuth、助手队列、组件及 Playwright 用户流程。浏览器测试验证桌面/移动布局、主要操作与工作台对比度。覆盖率口径不包含整个 UI。

助手使用模拟模型响应测试异常、取消、来源过期、撤权、限额与恢复；未声称完成真实提供商验证。CI 另配置 PostgreSQL 集成和镜像构建，需在托管环境执行后才能算目标部署验证完成。
